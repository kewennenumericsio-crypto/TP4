using LibVoiture;
            // Exemple d'utilisation (voir la consigne sur l'image)
            var r = new Reservoir(50);
            r.Remplir(30);
            Console.WriteLine(r.ToString());
       